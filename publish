#!/bin/bash

set -euo pipefail

if [[ -z "${PAGES_DEPLOY_URL-}" ]]; then
  echo 'Error: must set $PAGES_DEPLOY_URL' >&2
  exit 1
fi

set -x

./get-repos ./repos | ./build-site ./site

rsync -r site/ "$PAGES_DEPLOY_URL"
